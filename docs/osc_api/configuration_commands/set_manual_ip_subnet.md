# Set Manual IP Subnet

```
/sio/cfg/lan/manual/subnet/set
```

Sets the subnet mask prefix length for Manual IP mode.

### Arguments

- Arg 0 (`int` | `float`): Subnet mask prefix length (0-32)

## Effects
Updates the Manual IP subnet mask configuration.